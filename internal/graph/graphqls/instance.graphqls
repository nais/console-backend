type Instance implements Node {
  id: ID!
  name: String!
  state: InstanceState!
  message: String!
  image: String!
  restarts: Int!
  created: Time!
  log(tailLines: Int!): [LogLine!]!
}

enum InstanceState {
  RUNNING
  FAILING
  UNKNOWN
}
